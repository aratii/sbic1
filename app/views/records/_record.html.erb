<li>
  <%= link_to record.name, record %>

  <% if current_user.admin? %>
  | <%= link_to "delete", record, :method => :delete, :confirm => "Are you sure you want to delete this record?",
                                :title => "Delete #{record.name}" %>
  <% end %>
</li>
